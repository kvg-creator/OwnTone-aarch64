name: Owntone server
version: 0.2.0
slug: owntone-server
description: OwnTone is a DAAP (iTunes) and MPD media server with support for AirPlay, Chromecast, MPD, Spotify and internet radio.
arch:
  - aarch64
  - amd64
  - armv7

# 💡 NØKKELEN: bruk ferdig bygget offisielt image – da slipper vi all bygging
image: "docker.io/owntone/owntone:latest"

host_network: true

ports:
  3689/tcp: 3689

ports_description:
  3689/tcp: "Owntone web interface / DAAP"

# Du *kan* mappe share hvis du vil bruke /share inn i containeren senere,
# men det er ikke nødvendig for å få den opp å gå:
# map:
#   - share:rw

ingress: false
ingress_port: 0

webui: "http://[HOST]:[PORT:3689]/"

startup: services
boot: auto
init: false

options: {}
schema: {}
